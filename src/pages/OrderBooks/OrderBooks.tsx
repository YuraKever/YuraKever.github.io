import { FC } from "react";
import PlayContainer from "./components/PlayContainer/PlayContainer";

const OrderBooks: FC = () => {
  return (
    <main className="w-full min-h-screen h-full bg-gray-700 text-gray-100">
      <PlayContainer />
    </main>
  )
}

export default OrderBooks